# ADR-0003 · WebSocket 单通道而非多通道

**状态**：accepted

## 背景
Web 工作台需要消息、设备、任务等多类推送；最初考虑每类一个 WS。

## 决策
所有 Web 推送复用同一个 `/ws/web` 连接，按 `event` 字段路由。

## 理由
- 连接更少、心跳更简单
- 客户端订阅模型更直观
- 多浏览器 / 多 tab 时易做去重

## 取舍
- 高并发推送可能挤占同一连接；MVP1 量小不是问题；扩展期可改用 Redis fanout + worker。
